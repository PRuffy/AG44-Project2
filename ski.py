import networkx as nx

class skiDomain(object):
	"""docstring for skiDomain"""
	def __init__(self):
		numberOfEdge
		numberOfVertice
		domain = nx.MultiDiGraph()
		
	def createGraph(self, data):
		self.numberOfEdge = int(data[0][0])
		self.numberOfVertice = int(data[self.numberOfEdge+2][0])